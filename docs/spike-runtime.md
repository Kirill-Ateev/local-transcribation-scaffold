# Спайк рантайма на DGX Spark (design D1)

> Шаблон заполняется по факту выполнения задач 1.1–1.4 на машине.
> До заполнения основной путь считается неподтверждённым.

## Результат спайка

- Дата:
- Машина / ОС: DGX Spark, DGX OS ____ (Ubuntu 24.04)
- CUDA toolkit: ____
- Рецепт: https://github.com/paruparu/faster-whisper-dgx-spark @ commit ____
- Python: ____ ; ctranslate2: ____ ; faster-whisper: ____

## Выполненные шаги

```
# вставьте фактическую последовательность команд из инструкции рецепта
```

## Доказательства GPU-инференса (задача 1.2)

- Лог инициализации CUDA / sm_121:
  ```
  <вставить строки логов>
  ```
- `nvidia-smi` во время инференса (процесс и потребление памяти):
  ```
  <вставить вывод>
  ```
- `GET /health`: `backend.device=` ____ , `compute_type=` ____
- Время инференса контрольной фразы (10 c аудио): GPU ____ с vs CPU-оценка ____

## Решение об упаковке (задача 1.4)

- [ ] Docker-образ из рецепта (указать образ/tag: ____)
- [ ] venv + systemd (`deploy/transcribe.service`)
Обоснование:

## Фолбэк-лестница (задача 1.3, только если спайк провален)

- Причина провала основного пути:
- Следующий шаг лестницы: whisper.cpp `GGML_CUDA=ON` / PyTorch aarch64
- Команды и результат:

## Вывод

Основной путь подтверждён / применён фолбэк №____. Итоговая конфигурация сервиса:
`WHISPER_MODEL=SoybeanMilk/faster-whisper-Breeze-ASR-25 DEVICE=____ COMPUTE_TYPE=____`
