# Runtime + инструменты репозитория (тесты и eval) — ставится одной командой.
# Для DGX Spark (GB10) версии ctranslate2/faster-whisper берутся из рецепта
# спайка (docs/spike-runtime.md); эти минимумы проверены на CPU x86_64.
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
faster-whisper>=1.0
httpx>=0.27
pyyaml>=6.0
numpy>=1.26
pytest>=8.0
