# Tasks: local-voice-transcription-stack

> Развёртывание на целевых машинах (DGX Spark, MacBook), спайк рантайма и
> приёмочные сценарии владелец выполняет отдельно по инструкциям
> `deploy/README.md` (вариант A — рецепт faster-whisper-dgx-spark, вариант B —
> venv+systemd), `client/README.md`, `docs/spike-runtime.md` — в этом списке
> они не трекаются.

## 1. Каркас сервиса и базовая транскрибация

- [x] 1.1 Создать `server/`: FastAPI + Uvicorn, single-worker, конфигурация через переменные окружения (`server/.env.example`, `.env` в `.gitignore`, автозагрузка `.env`), загрузка единственной модели large-v3 при старте процесса. Verify: `./run.sh` стартует, веса загружаются один раз, в логах нет ошибок
- [x] 1.2 Реализовать `POST /v1/audio/transcriptions` (multipart): декодирование аудио → faster-whisper с `language=ru` по умолчанию, `language=en` переопределяется, `auto` означает автораспознавание, `condition_on_previous_text=False`; параметр `model` принимается для совместимости с протоколом и игнорируется как селектор; ответ `{"text", "language", "duration"}`; входные форматы wav/mp3/flac/m4a/aac/ogg/opus/webm и сырой PCM s16le mono 16 kHz. Verify: pytest покрывает все ветки; живой curl-обзор подтверждает ответы
- [x] 1.3 Обработка ошибок: 400 (неподдерживаемый формат, пустой/битый файл), 413 (сверх лимита размера), 503 + `Retry-After` (модель ещё грузится); единый JSON-конверт ошибок. Verify: pytest-кейсы возвращают ожидаемые коды и сообщения

## 2. VAD и длинные аудио

- [x] 2.1 Интегрировать silero-vad перед моделью: обрезка тишины, сегментация на куски ≤ 30 c с оверлеем; файл без речи → HTTP 200 с пустым `text`. Verify: тест с тишиной даёт `"[]"`; запись с паузами не галлюцинирует в рамках локального прогона
- [x] 2.2 Склейка сегментов в связный текст; транскрибация часового MP3 одним синхронным запросом без роста потребления памяти от длительности файла. Verify: `server/scripts/test_hour_long.py`: 61-минутный MP3 → HTTP 200 за ~9 c, duration=3660s, RSS delta ≤ 150 МБ
- [x] 2.3 Сериализация запросов FIFO-очередью (одновременные запросы обрабатываются по одному). Verify: тест двух параллельных запросов фиксирует непересекающиеся интервалы инференса

## 3. Авторизация и health

- [x] 3.1 Проверка `Authorization: Bearer <token>` из окружения на всех эндпоинтах кроме `/health`; отказ до запуска инференса; `GET /v1/models` для совместимости с OpenAI-клиентами. Verify: curl/тесты — 401 без и с неверным токеном, список моделей отдаёт единственную резидентную модель
- [x] 3.2 Реализовать `GET /health` без авторизации: признак готовности модели (loading/ready/error), версия, бэкенд (device/compute_type). Verify: сразу после старта loading, после прогрева ready; живой прогон подтверждает оба состояния

## 4. Eval-харнесс качества

- [x] 4.1 Написать `eval/score.py`: прогон набора через API (language/initial_prompt из CLI), подсчёт WER/CER, таблица результатов, артефакты прогона (токен в артефактах маскируется). Verify: юнит-тесты метрик зелёные; два последовательных прогона дали идентичные таблицы. Аудио фраз владелец записывает отдельно (`eval/phrases.yaml` готов к использованию)

## 5. Проверка репозитория

- [x] 5.1 Полный флоу «с чистого листа»: свежий venv → `pip install -r server/requirements.txt` → `cp .env.example .env` → `./run.sh` → health/loading→ready → `/v1/models` → транскрибации всех типов входа → коды ошибок → `deploy/smoke.sh`. Verify: живой прогон на локальной модели tiny прошёл полностью
- [x] 5.2 Выполнить `openspec validate --strict` для change. Verify: валидация проходит без ошибок

## 6. Интеграция с целевым рантаймом DGX Spark

- [x] 6.1 Подготовить запуск поверх рецепта `paruparu/faster-whisper-dgx-spark`: `deploy/docker-compose.spark.yml` (сборка Dockerfile рецепта, монтаж нашего `server/`, override команды на наш uvicorn, volume hf-cache, GPU-stanza, порт 8337) и переработанный `deploy/README.md` (вариант A — рецепт, основной; вариант B — venv+systemd, альтернатива). Verify: YAML разбирается без ошибок, пути/переменные сверены с README рецепта
